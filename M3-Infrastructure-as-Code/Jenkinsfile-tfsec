pipeline {
  agent {
    docker { 
        image 'tfsec/tfsec-ci:v1.13.2-arm64' 
        args "--entrypoint=''"
    }
  }
  stages {
    stage('Test') {
      steps {
        sh 'tfsec M3-Infrastructure-as-Code/non-compliant --no-color'
      }
    }
  }
}