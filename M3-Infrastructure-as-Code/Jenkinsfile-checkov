pipeline {
  agent {
    docker { image 'bridgecrew/checkov:2' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'checkov -d M3-Infrastructure-as-Code/compliant --quiet --compact'
      }
    }
  }
}